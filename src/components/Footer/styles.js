import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  root: {
    paddingTop: 48,
    color: "#fff",
    fontSize: 14,
    textAlign: "center",
    position: "relative",
    background: "rgb(27, 37, 46)",
  },
  bull: {
    marginRight: 5,
    marginLeft: 5,
  },
}));
